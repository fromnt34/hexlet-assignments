- content_for :title do
  = @post.title
p
  strong.me-2 Body:
  = @post.body
/ BEGIN
= render 'comments/partial/form', comment: @comment, url: post_comments_path(@post)
- @post.post_comments.each do |comment|
  p= comment.body
  = render 'comments/partial/actions', comment:
  hr

/ END
hr

.d-flex
  = link_to 'Edit', edit_post_path(@post), class: 'btn btn-outline-primary me-2'
  = link_to 'Back', :back, class: 'btn btn-outline-primary'
